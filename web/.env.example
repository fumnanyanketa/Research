# Copy to web/.env (local) and set the same two in the Vercel project env.
# Supabase → Project Settings → API. The anon key is browser-safe.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
